# .env.op — TEST FIXTURE. References only (op://vault/item/field). No values.
# Per DESIGN-secrets-and-keys.md Layer 3 — this is the `op run --env-file` format.
# The references below mirror the source: onepassword entries in keys.yaml. Tests
# that exercise the op path MOCK the `op` CLI — these references are never resolved
# against a real 1Password account.
WB_TEST_VERCEL_TOKEN=op://TestVault/Vercel/api_token
WB_TEST_STRIPE_SECRET_KEY=op://TestVault/Stripe/secret_key
